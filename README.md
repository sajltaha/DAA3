# MST Assignment: Optimization of City Transportation Network

This project implements Prim's and Kruskal's algorithms to find the Minimum Spanning Tree (MST) for a city's transportation network, as per Assignment 3.

## Overview
- Language: Java
- Build Tool: Maven
- Focus: Graph algorithms, performance analysis, testing.

## How to Build and Run
- Install Maven.
- Build: `mvn clean install`
- Run tests: `mvn test`
- Run main: (To be added later)

## Input/Output
- Input: JSON files in `src/main/resources` (e.g., input.json)
- Output: JSON files generated (e.g., output.json)